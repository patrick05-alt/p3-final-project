package com.uvt.newcomerassistant.demo.config;

// This class is disabled - CORS is now configured in SecurityConfig.java only
// Having multiple CORS configurations with conflicting allowCredentials caused 403 errors

// @Configuration
// public class CorsConfig implements WebMvcConfigurer {
//     // Removed - use SecurityConfig.corsConfigurationSource() instead
// }
